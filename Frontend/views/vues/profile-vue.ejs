<script>
    var app = new Vue({
        el: '#app',
        data: {
            userData: JSON.parse(`<%- userData %>`),
            bookList: [],
        },
        async mounted() {
            const response = await axios({
                method: "POST",
                url: "http://localhost:5000/owned-book/get/user/all",
                data: {
                    id: document.getElementById("id").value,
                },
            })
            .then(
                (response) => {
                    return response.data.payload.data;
                },
            );
            this.bookList = response;
        },
    });
</script>