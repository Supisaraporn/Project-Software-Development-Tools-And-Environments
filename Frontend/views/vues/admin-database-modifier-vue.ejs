<script>
    var app = new Vue({
        el: '#app',
        data: {
            selectedCollection: "default",
            nowCollection: [],

            bookModifier: "default",
            rewardModifier: "default",
            historyModifier: "default",
        },
        watch: {
            selectedCollection(next, pre) {
                axios
                .get("http://localhost:5000/"+next+"/get/all")
                .then(
                    (response) => {
                        this.nowCollection = response.data.payload.data;
                    },
                );
            },
        }
    });
</script>