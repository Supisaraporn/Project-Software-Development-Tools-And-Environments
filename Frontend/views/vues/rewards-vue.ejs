<script>
    var app = new Vue({
        el: '#app',
        data: {
            rewardList: [],
            nowShowModal: false,
            nowItemModal: [],
            userPoint: JSON.parse(`<%= userData.user_point %>`),
        },
        created() {
            axios
                .get("http://159.223.45.216:2513/reward/get/all")
                .then(
                    (response) => {
                        if (response.data.success) {
                            this.rewardList = response.data.payload.data;
                        } else {
                            console.log("Failed to get data.");
                        };
                    },
                );
        },
    });
</script>